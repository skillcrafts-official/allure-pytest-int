[project]
name = "allure-pytest-int"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "allure-pytest>=2.16.0",
    "pytest>=9.0.3",
]
